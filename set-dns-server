# dnsamsk.conf
addn-hosts=dnsmasq.hosts

# dnsmask.hosts
# (ip resolve), for example:
127.0.0.1 bla.site.com

# run using dnsmask
dnmask -C dnsmask.conf --no-daemon

# to test, you can use dig / host
dig @localhost bla.site.com
host bla.site.com 127.0.0.1
